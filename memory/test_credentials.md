# Test Credentials
# Agent writes here when creating/modifying auth credentials (admin accounts, test users).
# Testing agent reads this before auth tests. Fork/continuation agents read on startup.

## Garmin Connect (real account — backend .env only)
- GARMIN_USERNAME: mallegolbrieg@gmail.com
- GARMIN_PASSWORD: (stored in /app/backend/.env)
- Provider: gccli (token at /app/backend/.gccli_home)
